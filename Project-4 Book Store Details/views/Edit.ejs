<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Book Details [Edit]</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <style>
        body {
            background-color: #f8f9fa;
            background-image: url('https://img.freepik.com/premium-photo/many-stacks-educational-books-study-university-library-background_488220-17553.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }

        .card {
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .card-header {
            background: linear-gradient(135deg, #007bff, #0056b3);
            color: #fff;
            font-size: 1.5rem;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .form-label {
            font-weight: bold;
            color: #343a40;
        }

        .form-control {
            border-radius: 8px;
            border: 1px solid #ced4da;
            box-shadow: none;
        }

        .form-control:focus {
            border-color: #80bdff;
            box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
        }

        .form-select {
            border-radius: 8px;
            border: 1px solid #ced4da;
            box-shadow: none;
        }

        .form-select:focus {
            border-color: #80bdff;
            box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
        }

        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            border-radius: 50px;
            padding: 0.5rem 2rem;
            font-size: 0.9rem;
            text-transform: uppercase;
            font-weight: bold;
        }

        .btn-secondary {
            border-radius: 50px;
            padding: 0.5rem 2rem;
            font-size: 0.9rem;
            text-transform: uppercase;
            font-weight: bold;
            color: #6c757d;
            background-color: #f8f9fa;
            border: 1px solid #6c757d;
        }

        .btn-secondary:hover {
            background-color: #e2e6ea;
            border-color: #dae0e5;
        }
    </style>

</head>

<body>
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-lg-6">
                <div class="card shadow-sm">
                    <div class="card-header bg-primary text-white text-center">
                        <h1 class="h4">Edit Book Details</h1>
                    </div>
                    <div class="card-body">
                        <form method="post" action="/updateRecord">
                            <input type="hidden" name="editId" value="<%= edit.id %>">
                            <div class="mb-3">
                                <label for="name" class="form-label">Book Name</label>
                                <input type="text" class="form-control" name="name" value="<%= edit.bookName %>">
                            </div>
                            <div class="mb-3">
                                <label for="price" class="form-label">Book Price</label>
                                <input type="text" class="form-control" name="price" value="<%= edit.bookPrice %>">
                            </div>
                            <div class="mb-3">
                                <label for="pages" class="form-label">Book Pages</label>
                                <select class="form-select" name="pages">
                                    <% 
                                    pages = ["1 to 100", "1 to 150", "1 to 200", "More than 200"];
                                    pages.forEach((val) => { 
                                 %>
                                     <option value="<%= val %>" <%= edit.bookPages === val ? "selected" : "" %>>
                                         <%= val %>
                                     </option>
                                 <% }) %>
                                 
                                </select>
                            </div>
                            <div class="mb-3">
                                <label for="description" class="form-label">Book Description</label>
                                <input type="text" class="form-control" name="description"
                                    value="<%= edit.bookDescription %>">
                            </div>
                            <div class="mb-3">
                                <label for="author" class="form-label">Book Author</label>
                                <input type="text" class="form-control" name="author" value="<%= edit.bookAuthor %>">
                            </div>
                            <div class="text-center">
                                <button type="submit" class="btn btn-primary">Submit</button>
                                <a href="/viewRecord" class="btn btn-secondary">Cancel</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>