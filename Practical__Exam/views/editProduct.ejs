<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blogging</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <style>
        body {
            background-color: #f4f4f4;
            font-family: Cambria, Cochin, Georgia, Times, 'Times New Roman', serif;
        }

        .container {
            max-width: 600px;
            margin: 40px auto;
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        }

        .card {
            padding: 20px;
            background: #fff;
            border-radius: 8px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        }

        .btn-primary {
            background: #007bff;
            border: none;
            padding: 10px 15px;
            border-radius: 5px;
            font-size: 16px;
            transition: 0.3s;
        }

        .btn-primary:hover {
            background: #0056b3;
        }

        .btn-secondary {
            background: #6c757d;
            border: none;
            padding: 10px 15px;
            border-radius: 5px;
            font-size: 16px;
            transition: 0.3s;
        }

        .btn-secondary:hover {
            background: #545b62;
        }
    </style>
</head>
<body>

    <div class="container">
        <h2 class="text-center my-4">Edit Your Products</h2>

        <div class="card">
            <form action="/updatedata" method="post" enctype="multipart/form-data">
                <input type="hidden" name="editId" value="<%= oneRow?.id %>">

                <div class="mb-3">
                    <label class="form-label">Name:</label>
                    <input type="text" name="name" value="<%= oneRow?.name %>" class="form-control" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">Price:</label>
                    <input type="text" name="price" value="<%= oneRow?.price %>" class="form-control" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">Quantity:</label>
                    <input type="number" name="quantity" value="<%= oneRow?.quantity %>" class="form-control" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">Description:</label>
                    <textarea name="description" class="form-control" rows="4" required><%= oneRow?.description %></textarea>
                </div>

                <div class="mb-3">
                    <label class="form-label">Upload New Image:</label>
                    <input type="file" name="image" class="form-control" accept="image/*">
                    <img src="<%= oneRow?.image %>" width="100" class="image-preview" alt="Current Image">
                </div>

                <button type="submit" class="btn btn-primary">Update Products</button>
            </form>
        </div>

        <div class="text-center my-3">
            <a href="/viewproductpage" class="btn btn-secondary">View Products</a>
        </div>
    </div>

</body>
</html>
